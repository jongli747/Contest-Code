#include<stdio.h>
#include<math.h>
int main()

{
    float a,b,c,x,y,z,p,q;
    printf("Enter the value of lenghts: ");
    scanf("%f%f%f",&a,&b,&c);

    cos(x)==(b*b+c*c-a*a)/2*b*c;
    cos(y)==(a*a+c*c-b*b)/2*a*b;
    cos(z)==(a*a+b*b-c*c)/2*a*b;

    printf("cos(x) = %f\ncos(y) = %f\ncos(z) = %f",cos(x),cos(y),cos(z));
    return 0;
 }
