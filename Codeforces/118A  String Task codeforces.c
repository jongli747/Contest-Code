#include<stdio.h>

#include<string.h>

int main()

{
    char s[100];

    int i;

    gets(s);

    for(i=0;s[i]!='A'|| 'a' || 'E' || 'e' || 'I' || 'i' || 'O' || 'o';i++)
        printf(" ");






    puts(s);
}
