#include<bits/stdc++.h>
using namespace std;
 int t=0, numofobj, price, weight, num_of_people,max_weight;

int main()
{

    cin >> t;
    while(t--)
    {
        cin >> numofobj;
        for(int i=0; i<numofobj; i++)
        {
            cin >> price >> weight;

        }
    cin >> num_of_people >> max_weight;
    }

}
